language: cpp
compiler:
  - clang
  - gcc
branches:
  only: 
    - ci
addons:
  apt:
    packages:
    - python-pip
    - libibus-1.0-dev
before_script:
  - pip install cpplint
script:
  - g++ --version
  - cpplint *
  - gcc levena.c $(pkg-config ibus-1.0 --cflags --libs)